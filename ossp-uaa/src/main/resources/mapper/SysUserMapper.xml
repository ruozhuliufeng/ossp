<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.aixuxi.ossp.uaa.mapper.SysUserMapper">

    <select id="listByPermId" resultType="cn.aixuxi.ossp.uaa.entity.SysUser">
        SELECT DISTINCT
        u.*
        FROM sys_user_role sur
        LEFT JOIN sys_role_permission srp on sur.role_id = srp.role_id
        LEFT JOIN sys_user u on u.id = sur.uid
        WHERE srp.permission_id = #{id}
    </select>
</mapper>
